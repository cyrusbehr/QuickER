// import { fromJS } from 'immutable';
// import { selectIncomingRequestContainerDomain } from '../selectors';

describe('selectIncomingRequestContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
