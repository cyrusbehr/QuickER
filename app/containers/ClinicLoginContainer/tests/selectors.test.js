// import { fromJS } from 'immutable';
// import { selectClinicLoginContainerDomain } from '../selectors';

describe('selectClinicLoginContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
