// import { fromJS } from 'immutable';
// import { selectHospitalLoginContainerDomain } from '../selectors';

describe('selectHospitalLoginContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
