// import { fromJS } from 'immutable';
// import { selectHospitalRegistrationContainerDomain } from '../selectors';

describe('selectHospitalRegistrationContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
