// import { fromJS } from 'immutable';
// import { selectClinicRegistrationContainerDomain } from '../selectors';

describe('selectClinicRegistrationContainerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
