/*
 *
 * ClinicRegistrationContainer constants
 *
 */
const SCRAPED_CLINICS_ROUTE = `${window.location.origin}/api/scrapedclinics`;

const DEFAULT_ACTION = 'app/ClinicRegistrationContainer/DEFAULT_ACTION';

module.exports = {
  SCRAPED_CLINICS_ROUTE,
  DEFAULT_ACTION,
};
